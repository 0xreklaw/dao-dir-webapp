import React from 'react'
//  components
import Layout from '@/components/Layout'

const index = () => {
  return (
    <Layout>
      <h3>Webapp</h3>

      {/* If Connected, then show profile page, else show directory page */}
    </Layout>
  )
}

export default index